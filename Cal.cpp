#include <stdio.h>

// --- Prototypes (ประกาศฟังก์ชันไว้ก่อน) ---
int add(int a, int b);
int sub(int a, int b);
int mul(int a, int b);
float divide(int a, int b);

int main() {
    int num1, num2;
    float result;
    char op;

    return 0;
}

